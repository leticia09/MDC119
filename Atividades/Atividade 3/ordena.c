#include <stdio.h>
#include <stdlib.h>

int maior(int a, int b){
return a>b;

}
void ordena (int *p, (int*)funcao(int *a, int *b)){
    for(int i=0; i<10; i++){
        for(int j=i; j<10; j++){
            if(funcao(p[i]), (p[j])){
                int aux = p[i];
                p[i]=p[j];
                p[j]=aux;

            }
        }
    }
}
void imprime(int *v){
    for(int i=0;i<10;i++){
        printf("%d", v[i]);
    }
}

int main()[
    int vetor[]={5,6,9,8,1,3,6,2,4};
    
    orderna(vertor, maior);
    
    imprime(vetor);

    ordena(vetor, manor);

    imprime(vetor);

    return 0;
